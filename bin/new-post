#!/bin/sh -eu

TITLE="$*"
SLUG="$(echo $TITLE | tr A-Z a-z | tr -s ' ' '-')"
git co -b "topic/${SLUG}"

cat <<EOF > "site/posts/${SLUG}.md"
---
title: $TITLE
author: Ryan James Spencer
date:
tags: []
---
EOF

git a .
git c -m "${TITLE}"
